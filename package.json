{
  "name": "jira-toggl-quickstart",
  "version": "0.3.0",
  "private": true,
  "description": "Chrome extension for starting Toggl Track timers from Jira issues or a manual popup entry.",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check options.js && node --check popup.js && node -e \"JSON.parse(require('node:fs').readFileSync('manifest.json', 'utf8'))\"",
    "test": "node --test tests/*.test.cjs",
    "validate": "npm run check && npm test"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cleissonom/jira-toggl-quickstart.git"
  },
  "bugs": {
    "url": "https://github.com/cleissonom/jira-toggl-quickstart/issues"
  },
  "homepage": "https://github.com/cleissonom/jira-toggl-quickstart#readme",
  "engines": {
    "node": ">=20"
  }
}
